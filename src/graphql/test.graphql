#query get ($shortCode: String!) {
#    getShortenedUrl(shortCode: $shortCode) {
#        id
#        originalUrl
#        shortCode
#        customShortCode
#        expiryPeriod
#        createdAt
#        expiresAt
#    },
#}
mutation mut {
    createShortenedUrl(
        originalUrl: "https://www.google.com",
    ) {
        id
        originalUrl
        shortCode
        customShortCode
        expiryPeriod
        createdAt
        expiresAt
    }
}
